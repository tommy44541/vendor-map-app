Fixes:
- App often kicked users back to login on open (concurrent refresh
  requests racing the rotating token, triggering backend re-use
  detection that revoked the whole family). Refresh is now
  single-flight so parallel 401s share one in-flight refresh.
